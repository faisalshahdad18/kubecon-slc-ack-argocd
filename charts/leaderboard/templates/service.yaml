apiVersion: v1
kind: Service
metadata:
  name: {{ template "helm-scoreboard.fullname" . }}
  labels:
    app: {{ template "helm-scoreboard.name" . }}
    chart: {{ template "helm-scoreboard.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helm-scoreboard.name" . }}
    release: {{ .Release.Name }}
